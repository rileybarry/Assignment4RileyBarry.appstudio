/*
var arr = ['one','two','three','one','one','two','four']

for(i=0;i<arr.length;i++)
  if (arr.includes(arr[i], i + 1))
    console.log('contains variable')
  else
    console.log('does not contain variable')
*/